---
name: elm-phantom-pass1-execution
description: "elm-phantom pass-1 COMPLETE+MERGED; pass 2 via planning/execution/PASS2-STATE.md — chunk 3 (2026-07-21): collision policy IMPLEMENTED (elm-cem branch collision-policy, 6 commits, all suites green, m3e byte-identity first-hand); shoelace regen DONE, warp partial (gate c missing), fluent blocked on K7 generator bug, web-awesome blocked on NEW attr-pipe collision class = OPEN JACK DECISION"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4f661b02-e973-4c2c-826a-8d4b13cb3b9b
---

Executing `planning/execution/2026-07-20-fable-max-prompt.md` (authoritative; 3 checkpoints). Related: [[elm-phantom-relayering-plan]], [[elm-cem-codegen-overhaul]].

**State 2026-07-20 late:**
- **S1 DONE + checkpoint-1 APPROVED**: `elm-html-intermediate-representation` repo (main), 6 modules `HtmlIr.{Element,Node,Attribute,Value,Kind,Internal}`; two-row Element; ONE curated Internal (no data ctors); keyedNode added on Jack's order; structural map (VirtualDom.mapAttribute); verify/ oracle 10/10; opinionated ops = lever compositions (placeSlot/coerce/delegate proven).
- **S2 DONE + checkpoint-2 APPROVED**: `planning/execution/2026-07-20-config-primitive-catalog.md` — 10 primitives (new: `parents` default-OPEN [transpose ≠ source — key correction], `_sets` @refs, `roles`); Jack's answers: Ctx demand ALWAYS, general refs specific aliases, native pkg name deferred to S4, _actions stays m3e-only.
- **S3 CORE DONE** (worktree `elm-cem-phantom`, branch `phantom-overhaul`, commits S3a-d): `Generate/Phantom/{Model,Emit}.elm` raw-string emitters behind `_config._phantom` flag (legacy path untouched-green, 361 tests); 13/13 byte-exact goldens + 9/9 acid (`tests/phantom/gate.mjs`, in npm test); Design-A BOTH families (view/el + build pipe w/ Available→Used in `<Lib>.Kind`); alias-RECORDING in bin/elm-cem.js (type.aliasName kept, union still resolved); Cem.TypeInfo grew aliasName (test fixtures patched). Deferred: injectRuntime deletion + old-emitter/tests deletion → after S5 m3e flip; Facts emission → S7; typed event decoders + AEnumMap/AEnumNum + _actions wiring → S5.
- **S4 DONE + checkpoint-3 APPROVED** (incl. name `jackhp95/elm-typed-html` + 3 warts approved: conflict-suffixing→S5, no build-family in homes, minimal native events→S5): repo `elm-typed-html` (main `aa35c00`) — 112 elements/27 modules generated from manifest+config (agent-built data, I finished); verify 9/9 (htmlia ports); whole surface+IR `Success! Compiled 29 modules`; docs probe 370,441 B = 48% cap ONE package. Worktree commit `bc76568` = 6 emitter hardenings (set-ref qualification, enum-anywhere union setters, role/state token suffix collisions, canonical-typed re-exports via reExportBlock, ctor-collision skip, main→main_). Home path: member-prefixed aliases, view-only. Data curations: widget roles OUT of div/span gates; `shared:text` added to @phrasing/@flow slots.
- **S5a DONE**: M3e full brand COMPILES — worktree `elm-m3e-phantom` branch `phantom-migration` (`1402e41`): config migrated (script in git history; _kinds primitive for action-wrapper/html aux kinds; static base-slot grafting no-op-verified), src/ = 129 modules on HtmlIr.*, app-harness `Success! Compiled 136 modules`. elm-cem worktree `68f64a5`: legacy-normalize front-end reuse + conditional rename (M3eButtonElement→Button, R2 names kept), merge key (tag,name) so R2 survives real merges, safeField intake, tokenIdent, union-typed pipes, required_ binder, withXSlot collision suffix, LOCAL member-typed setters on cross-component scalar conflicts (rows are Supported ⇒ sound). Docs probe mono+IR = **1,058,486 B > 768KB cap** → per Jack's locked #5: STATIC SPLIT AT PUBLISH (Stage F pass 2; elm-cem `split` machinery exists); down from ~2MB legacy.
- **S5 DONE** (b: Action system roster-emitted on IR, el/build integrated, wrapper=one Ir.node, acid-verified incl WrongCap rejection; c: typed event decoders from config {path,type} + on<Name>With escapes). Deferred: `group` convenience modules, examples docs projection (→ split/publish work).
- **S6 DONE**: docs app MIGRATED — full compile exit 0, pnpm build:ci exit 0, **Playwright 146/146**, ZERO Unsafe shims; playbook (`elm-m3e-phantom/docs-playbook/consumer-migration-playbook.md`) validated+hardened by two agent passes (both died at token budget mid-run BUT the commit-early+MIGRATION-STATE.md protocol made recovery trivial — use that protocol for all long mutating agents); Kit rebuilt as userland-on-phantom; `_legacyHtml`→`<Lib>.Unsafe.fromHtml` escape; member-classification-wins fix (M3e.Icon.name free String).
- **S7a DONE**: injectRuntime + rename + copyMarkupHandwritten DELETED from bin/elm-cem.js; legacy compile-gate.mjs retired (phantom gate = THE compile gate); suite 361 + phantom ALL GREEN + split green. Config-primitive reference homed at elm-cem-phantom/docs/config-primitives.md.
- **REVIEW-GATE FIXES DONE (2026-07-21)**: **B1** ✅ TypedHtml regen byte-clean (13 stale files were pre-S6b) + `scripts/regen-diff-gate.sh`. **B2** ✅ (agent, verified): examples generators retargeted to phantom surface (to-elm.mjs → general surface; gen-record-build + gen-barrel = no-ops pending rule updates; usage.spec.ts updated) — FULL `npm run build` exit 0 + `CI=1 playwright` 146/146 both SELF-verified; 5 pre-existing lib-test fails (Fix C childSlotByKind oracle) noted. **P3/P5** ✅. **S7b lockstep** ✅: Facts projection emitted (Cem.Facts schema, `shared:<role>` vocab, Permissive unlisted); fence probe-verified both directions; MRA rule accepts any `*.Aria` axis (review-cem worktree `elm-review-cem-phantom` branch `phantom-lockstep` `5573729`, 180/180); review gate on docs app **565→0 findings** (`I found no errors!`), incl. true-positive Seam.recast removal + fix-all unused imports. m3e worktree HEAD `48f0a2e`.
- **LEGACY DELETION DONE (agent, ALL SELF-VERIFIED 2026-07-21)**: phantom = THE pipeline (non-phantom config → "LEGACY PIPELINE RETIRED" error, negative-control verified); 16 legacy emitters + 21 dead tests + Acme fixtures deleted (75 surviving tests green); Config/Types trimmed; markup/ → data/native-attrs.json + native-manifest-gen/ (paths fixed); npm test exit 0 full chain; typed-html regen-diff-gate OK; m3e regen 0-diff byte-stable. elm-cem HEAD `f3aa37f`. typed-html HEAD `60e7f79` (Review/Facts committed). **PASS 1 COMPLETE — all review-gate blockers+minors+S7b closed; awaiting Jack's merge review.**
- **Deferred to Stage F/pass 2**: IR publish → package-form `elm make src/M3e.elm` + docs-cap split (m3e mono 1,058,486B > 768KB → 2-way static split); record/build + barrel example variants (need TranslateTo*/PreferBarrel rule updates for phantom); per-component API gaps from B2 report (FormField.control, Button.selectedSlot, DrawerContainer.startSlot/endSlot oracle issues).
- **SESSION HAZARDS logged as frictions**: shell-cwd silent resets (wrong-repo commit on review-cem main — repaired via reset; adopt absolute-cd discipline in EVERY mutating command); zsh no-word-split `set -- $S` loop (empty `$3` → `rm -rf tests/phantom/` — restored from git); pipe-exit swallowing (`cmd | tail` hides failures — check exits explicitly); elm-review stale per-file result cache across config edits (rm -rf review/elm-stuff).
- Branches for Jack's merge review: elm-cem-phantom `phantom-overhaul` · elm-m3e-phantom `phantom-migration` · elm-review-cem-phantom `phantom-lockstep` · elm-typed-html main · elm-html-intermediate-representation main.
- Gotchas: elm-codegen exits 0 on failure (run CLI directly); worktrees need node_modules symlink; elm-m3e CEM path only in MAIN checkout (docs/node_modules); compile harness = temp app with source-dirs [src, IR/src]; docs probe = temp package with IR copied in.
- Verify env: elm 0.19.1 at `elm-m3e/node_modules/.bin/elm`; acid apps use source-directories incl. `/Users/jack/Documents/code/elm-html-intermediate-representation/src`; elm-codegen exits 0 on compile failure (run CLI directly to see errors); worktree needed node_modules symlink.

**PASS 2 (state file = `planning/execution/PASS2-STATE.md`, planning/ is NOT a git repo — file-on-disk is the handoff):** chunks 1 + 1.5 merged. **Chunk 2 (2026-07-21): 5 repo-brands migrated to phantom pipeline on per-repo `pass2-phantom` branches — elm-calcite ALL GATES GREEN (first-hand verified; mergeable; 6-pkg split all ≤700KB); shoelace/warp/fluent-ui/web-awesome deterministic but compile-BLOCKED by a systemic elm-cem identifier-collision class (5 instances: `_top`/`top` token, global-vs-CEM `id` double-emit, `error`/`wa-error`→`onError`, `with-hint` attr vs `withX` builder, empty-enum `exposing ( )`); naive dedup WRONG for 2 instances (semantically distinct pairs) → rename policy = design-bearing, needs planning session; m3e's CEM triggers none of it (golden suite blind — add hostile fixture brand). HOLD the 4 branches (their mains still compile pre-phantom).** Clone-only carbon/ionic/spectrum/material-web not started (no repos; analyzer configs in elm-cem/cem-configs; spectrum glob broken; output location = open Q for Jack). Tick-down: opus × 5 multi-stage brand agents, all clean, no retries.

**CHUNK 3 (2026-07-21, collision-policy EXECUTION — signed off all 4 §8 decisions, then implemented):** elm-cem branch `collision-policy` (worktree `.worktrees/elm-cem-collision-policy`, 6 commits `d89da55..49d442a`): §7 steps 1-5 done — resolution in Model.resolve, fail-loud guard (rich errors: module+ident+raw sources+`_renames` snippet), K6 omission, `_renames` all 4 scopes, hostile/barren/expectError suites, m3e byte-identity VERIFIED FIRST-HAND (132 files, diff empty). Bonus find: case-variant tokens (`AUTO`/`auto`) were emitting duplicate decls on main → now fail-loud. Step 6: shoelace ✅ `b1f565d`; warp ⚠️ `cfccb55` (gate c never run — no acid harness in repo); fluent ⛔ K7 rule doesn't fire on real CEM (hostile passes — impl bug, branch reset, attempt preserved `pass2-phantom-fl-attempt1`); web-awesome ⛔ 11 collisions are attr-pipe-vs-attr-setter (taxonomy K5 row MISCLASSIFIED them; §6 "no config needed" wrong) → OPEN JACK DECISION: `_renames` taste pick vs new deterministic rule vs hold (attempt w/ INVERTED naming rejected, preserved `pass2-phantom-wa-attempt1`). Tick-down lesson (frictions logged): haiku executes mechanical work fine but bypasses STOP-conditions and skips verification deliverables — floor those at sonnet. All details in PASS2-STATE.md chunk-3 section.

**COLLISION-POLICY PLAN (2026-07-21, fable planning session — DESIGN, since signed off + implemented, see CHUNK 3): `planning/execution/2026-07-21-collision-policy.md`.** Taxonomy expanded to K1–K7 (K7 = NEW, found this session: fluent barrel `text` `_atoms.text` atom vs `fluent-text` element ctor, Fluent.elm L601/L671; the chunk-2 scan agent's C-labels were wrong — plan doc supersedes). Unifying rule: lossy-normalization side reverts on collision (`_top`→`top_` UNCONDITIONAL, `wa-error`→`onWaError`, `fluent-text`→`fluentText`, slot builder→`withXSlot`); true dups (global-vs-CEM `id`, intra-CEM repeats) collapse; fail-loud namespace guard at exposeBlock choke points; `_renames` config escape (extends existing `elmNameOverride` seed at Attr.elm:118). Hostile fixtures ×2 (`hostile` K1-K5+K7, `barren` K6) + gate.mjs `expectError` mode; m3e must regen byte-identical. 4 open Qs for Jack (§8: K1 unconditional?, K4 direction?, K7 deterministic-vs-fail-loud?, `_renames` schema). No brand needs config overrides under recommended policy. Impl tiers recorded in doc §7 (emitter=opus, fixtures/config/re-regens=sonnet — re-regen is single-stage, chunk-2's opus calibration doesn't apply).
