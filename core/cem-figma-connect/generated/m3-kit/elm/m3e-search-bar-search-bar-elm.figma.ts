// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=52977-33813
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-search-bar -> M3e.Component.SearchBar.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Search bar" (52977:33813).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * text content -> M3e.text (userland SEAM, not a library export or a bundle fact; see profiles/m3-kit/profile.json's elm.textSeam — NOT the golden spike's stale "M3e.text").
 * children -> rendered from examples.json / inline example into the M3e.Component.SearchBar.component child list (defect D fix).
 * example child (not emitted): <input> slot="input" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Pressed)
 * axis (unmapped): Show avatar — no CEM enum attribute shares its value set (options: False, True)
 * prop (mapped, not bound in this Elm surface): Show 1st trailing icon -> slot:trailing (kind literalIcon)
 * prop (mapped, not bound in this Elm surface): Placeholder text -> slot:input (kind text)
 * prop (mapped, not bound in this Elm surface): Show leading icon -> slot:leading (kind literalIcon)
 * prop (unmapped): Show 2nd trailing icon — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.Component.SearchBar.component
    { content = M3e.Component.SearchBar.leading (M3e.Icon.icon M3e.Icon.menu [] [])
    , action = M3e.Action.none
    }
    [ 
    ]
    [ M3e.Component.SearchBar.trailing (M3e.Icon.icon M3e.Icon.search [] [])
    ]`,
  imports: ["import M3e.Action", "import M3e.Component.SearchBar", "import M3e.Icon"],
  id: "m3e-search-bar-search-bar-elm",
  metadata: {
    nestable: true,
  },
}
