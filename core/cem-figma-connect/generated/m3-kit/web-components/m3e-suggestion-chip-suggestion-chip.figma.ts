// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=53923-28679
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (src/emit/html-label.mjs) — do not edit by hand.
 * m3e-suggestion-chip, bound to Figma set "Suggestion chip" (53923:28679).
 * axis: Style -> variant
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Focused, Pressed, Dragged, Disabled)
 * axis (unmapped): Selected — no CEM enum attribute shares its value set (options: False, True)
 * axis (unmapped): Show icon — no CEM enum attribute shares its value set (options: False, True)
 * prop: Label text -> content
 * prop: Leading icon -> slot:icon (instanceSwap)
 * prop (unmapped): Show focus indicator — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const variant = instance.getEnum("Style", {
  Outlined: "outlined",
  Elevated: "elevated",
})

export default {
  example: figma.code`<m3e-suggestion-chip variant="${variant}"><span>Label</span></m3e-suggestion-chip>`,
  imports: ["import \"@m3e/web/all\""],
  id: "m3e-suggestion-chip-suggestion-chip",
  metadata: {
    nestable: true,
  },
}
