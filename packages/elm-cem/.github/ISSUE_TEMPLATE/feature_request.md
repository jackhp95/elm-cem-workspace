---
name: Feature request
about: Propose new capability or an API change
title: "[feat] "
labels: enhancement
---

## Problem

<!-- What are you trying to do that you can't do today? Describe the use case, not
     the solution. Remember: elm-cem is design-system agnostic — a feature that only
     makes sense for one component library belongs in a config key, not the generator. -->

## Proposed API / behavior

<!-- Sketch the ideal shape. Is it a new `--config-from` key, a CLI flag, or a change
     to the generated output? Show the config JSON or the Elm signatures you'd want. -->

## Alternatives considered

<!-- What did you try with the existing config keys? Why doesn't the current API cover it? -->

## Scope / breaking change?

<!-- Would this change either public surface (the exposed Elm modules or the generated
     output)? See the stability policy in CHANGELOG.md. -->
