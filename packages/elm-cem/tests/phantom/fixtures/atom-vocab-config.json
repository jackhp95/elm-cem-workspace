{
  "_phantom": true,
  "_brand": "Av",
  "_comment": [
    "RC5: the cross-library shared-atom vocabulary is CLOSED.",
    "",
    "A `Shared`-marked row field is the only thing in this system that unifies across",
    "package boundaries, so its spelling is a contract between brands that never see",
    "each other's source. `shared:` used to be an open namespace: any string minted a",
    "field, and a typo minted a field no other brand would ever name — a private kind",
    "wearing cross-library clothes. That is exactly the `\"html\"` defect (RC5), where a",
    "slot advertised 'arbitrary HTML goes here' and no native element could satisfy it.",
    "",
    "This fixture exercises EVERY entry point a shared role has:",
    "  - `Card.body.kinds` — the SLOT side, misspelling `shared:phrasing`.",
    "  - `Label.kind`      — the PRODUCER side, inventing `shared:caption`.",
    "  - `_atoms`          — the ATOM side, misspelling `text` as `txet`.",
    "  - `_coerce[].to`    — the CROSSING side, misspelling `shared:phrasing` again.",
    "",
    "The crossing side is the one that hid longest: `_coerce` is validated only for its",
    "`from` component, and `Generate/Types.elm` claims a `shared:` target is resolved by a",
    "`slotKindMarkerT` that does not exist in this repo — so before this fixture the",
    "emitter wrote the raw string into a type annotation (`shared:phrasing : Brand`) and",
    "the run failed downstream, in generated Elm, with no mention of the config key.",
    "",
    "Fix any one spelling and that error goes away; fix all four and this fixture STOPS",
    "failing. The closed vocabulary is the load-bearing part."
  ],
  "_atoms": { "text": {}, "txet": {} },
  "_coerce": [
    {
      "from": "Label",
      "fromKind": "sharedCaption",
      "to": "shared:phrasin",
      "name": "asPhrasing"
    }
  ],
  "Card": {
    "admits": {
      "body": { "kinds": ["shared:text", "shared:phrasng"], "multi": true }
    }
  },
  "Label": {
    "kind": "shared:caption",
    "admits": { "unnamed": { "kinds": ["shared:text"], "multi": true } }
  }
}
