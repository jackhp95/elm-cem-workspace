// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=51593-5254
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-nav-item -> M3e.NavItem.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Building Blocks / Nav item" (51593:5254).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.NavItem.component child list (defect D fix).
 * axis: Selected -> selected
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Focused, Pressed)
 * axis (unmapped): Show icon — no CEM enum attribute shares its value set (options: False, True)
 * prop: Label text -> content
 * prop (mapped, not bound in this Elm surface): Badge label text -> content (kind text)
 * prop (mapped, not bound in this Elm surface): Icon -> slot:icon (kind instanceSwap)
 * prop (unmapped): Show badge label — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const selected = instance.getEnum("Selected", {
  True: "True",
  False: "False",
})

export default {
  example: figma.code`M3e.NavItem.component
    [ M3e.NavItem.selected ${selected}
    ]
    [ M3e.NavItem.icon (M3e.Icon.icon M3e.Icon.stars [ M3e.Icon.filled True ] [])
    , Kit.text "Label"
    ]`,
  imports: ["import Kit", "import M3e.Icon", "import M3e.NavItem"],
  id: "m3e-nav-item-building-blocks-nav-item-elm",
  metadata: {
    nestable: true,
  },
}
