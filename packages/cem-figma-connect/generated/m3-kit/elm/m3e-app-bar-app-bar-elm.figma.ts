// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=58114-20565
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-app-bar -> M3e.Component.AppBar.component (elmSurface: "top", surface Standard).
 * bound to Figma set "App bar" (58114:20565).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Component.AppBar.component child list (defect D fix).
 * axis (unmapped): Configuration — no CEM enum attribute shares its value set (options: Small-centered, Small-image, Search, Small, Medium, Large)
 * axis (unmapped): Elevation — no CEM enum attribute shares its value set (options: Flat, On-scroll)
 * prop (unmapped): Image — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show 1st trailing action — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show 2nd trailing action — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show 3rd trailing action — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.Component.AppBar.component
    [ 
    ]
    [ M3e.Component.AppBar.leading (M3e.Component.IconButton.component { content = M3e.Icon.icon M3e.Icon.menu [] [], action = M3e.Action.none } [] [])
    , M3e.Component.AppBar.title (M3e.text "Title")
    , M3e.Component.AppBar.trailing (M3e.Component.Avatar.component [] [ M3e.text "A" ])
    ]`,
  imports: ["import M3e", "import M3e.Action", "import M3e.Component.AppBar", "import M3e.Component.Avatar", "import M3e.Component.IconButton", "import M3e.Icon"],
  id: "m3e-app-bar-app-bar-elm",
  metadata: {
    nestable: true,
  },
}
