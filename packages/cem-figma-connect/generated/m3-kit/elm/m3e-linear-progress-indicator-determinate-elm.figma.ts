// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=58005-7997
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-linear-progress-indicator -> M3e.LinearProgressIndicator.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Linear-determinate progress indicator" (58005:7997).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.LinearProgressIndicator.component child list (defect D fix).
 * axis (unmapped): Type — no CEM enum attribute shares its value set (options: Flat, Wave)
 * axis (unmapped): Thickness — no CEM enum attribute shares its value set (options: 4 dp, 8 dp)
 * axis (unmapped): Progress — no CEM enum attribute shares its value set (options: 0, 10, 20, 50, 80, 100)
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.LinearProgressIndicator.component
    [ M3e.LinearProgressIndicator.mode M3e.Values.determinate
    , M3e.LinearProgressIndicator.value 70
    ]
    []`,
  imports: ["import M3e.LinearProgressIndicator", "import M3e.Values"],
  id: "m3e-linear-progress-indicator-determinate-elm",
  metadata: {
    nestable: true,
  },
}
