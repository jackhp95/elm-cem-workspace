// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=53923-27888
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-input-chip -> M3e.InputChip.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Input chip" (53923:27888).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * text content -> Kit.text (userland SEAM, not a library export or a bundle fact; see profiles/m3-kit/profile.json's elm.textSeam — NOT the golden spike's stale "M3e.text").
 * children -> rendered from examples.json / inline example into the M3e.InputChip.component child list (defect D fix).
 * axis (unmapped): Configuration — no CEM enum attribute shares its value set (options: Label only, Label & leading icon, Label & avatar)
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Focused, Dragged)
 * axis (unmapped): Selected — no CEM enum attribute shares its value set (options: False, True)
 * axis (unmapped): Show closing icon — no CEM enum attribute shares its value set (options: false, true)
 * prop: Label text -> content
 * prop (mapped, not bound in this Elm surface): Leading icon -> slot:icon (kind instanceSwap)
 * prop (unmapped): Avatar — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show focus indicator — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.InputChip.component
    { content = Kit.text "Label"
    , action = M3e.Action.none
    }
    [ 
    ]
    []`,
  imports: ["import Kit", "import M3e.Action", "import M3e.InputChip"],
  id: "m3e-input-chip-input-chip-elm",
  metadata: {
    nestable: true,
  },
}
