// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=54446-25289
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-switch -> M3e.Switch.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Switch" (54446:25289).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Switch.component child list (defect D fix).
 * axis: Selected -> checked
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Focused, Pressed, Disabled)
 * axis (unmapped): Icon — no CEM enum attribute shares its value set (options: False, True)
 * prop (unmapped): Show focus indicator — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const checked = instance.getEnum("Selected", {
  False: "False",
  True: "True",
})

export default {
  example: figma.code`M3e.Switch.component
    [ M3e.Switch.checked ${checked}
    ]
    []`,
  imports: ["import M3e.Switch"],
  id: "m3e-switch-switch-elm",
  metadata: {
    nestable: true,
  },
}
