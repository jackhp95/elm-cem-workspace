// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=57998-43095
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-fab -> M3e.Fab.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Extended FAB" (57998:43095).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * text content -> Kit.text (userland SEAM, not a library export or a bundle fact; see profiles/m3-kit/profile.json's elm.textSeam — NOT the golden spike's stale "M3e.text").
 * children -> rendered from examples.json / inline example into the M3e.Fab.component child list (defect D fix).
 * axis: Color -> variant
 * axis: Size -> size
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Focused, Pressed)
 * prop (mapped, not bound in this Elm surface): Icon -> slot: (kind instanceSwap)
 * prop (unmapped): Show focus indicator — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const variant = instance.getEnum("Color", {
  "Primary container": "M3e.Values.primaryContainer",
  "Secondary container": "M3e.Values.secondaryContainer",
  "Tertiary container": "M3e.Values.tertiaryContainer",
  Primary: "M3e.Values.primary",
  Secondary: "M3e.Values.secondary",
  Tertiary: "M3e.Values.tertiary",
})

const size = instance.getEnum("Size", {
  Default: "M3e.Values.small",
  Medium: "M3e.Values.medium",
  Large: "M3e.Values.large",
})

export default {
  example: figma.code`M3e.Fab.component
    { content = M3e.Icon.icon M3e.Icon.stars [] []
    , action = M3e.Action.none
    }
    [ M3e.Fab.extended True
    , M3e.Fab.variant ${variant}
    , M3e.Fab.size ${size}
    ]
    [ M3e.Fab.label (Kit.text "Label")
    ]`,
  imports: ["import Kit", "import M3e.Action", "import M3e.Fab", "import M3e.Icon", "import M3e.Values"],
  id: "m3e-fab-extended-elm",
  metadata: {
    nestable: true,
  },
}
