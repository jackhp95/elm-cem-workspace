// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=58005-8525
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-circular-progress-indicator -> M3e.CircularProgressIndicator.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Circular-indeterminate progress indicator" (58005:8525).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.CircularProgressIndicator.component child list (defect D fix).
 * axis (unmapped): Type — no CEM enum attribute shares its value set (options: Flat, Wave)
 * axis (unmapped): Thickness — no CEM enum attribute shares its value set (options: 4 dp, 8 dp)
 * axis (unmapped): Progress — no CEM enum attribute shares its value set (options: 0, 10, 30, 50, 80, 100)
 * prop (unmapped): Show track — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.CircularProgressIndicator.component
    [ M3e.CircularProgressIndicator.indeterminate True
    ]
    []`,
  imports: ["import M3e.CircularProgressIndicator"],
  id: "m3e-circular-progress-indicator-circular-indeterminate-progress-indicator-elm",
  metadata: {
    nestable: true,
  },
}
