// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=58008-11810
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-slider -> M3e.Slider.view (elmSurface: "top", surface Standard).
 * bound to Figma set "Range slider" (58008:11810).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Slider.view child list (defect D fix).
 * axis: Size -> size
 * axis (unmapped): Orientation — no CEM enum attribute shares its value set (options: Horizontal, Vertical)
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Pressed, Disabled)
 * axis (unmapped): Value — no CEM enum attribute shares its value set (options: 0, 50, 100)
 * prop (mapped, not bound in this Elm surface): Icon -> slot: (kind instanceSwap)
 * prop (unmapped): Show value indicator — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show stops — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show icon — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const size = instance.getEnum("Size", {
  Large: "M3e.Values.large",
  XLarge: "M3e.Values.extraLarge",
  Medium: "M3e.Values.medium",
  Small: "M3e.Values.small",
  XSmall: "M3e.Values.extraSmall",
})

export default {
  example: figma.code`M3e.Slider.view
    [ M3e.Slider.size ${size}
    ]
    [ M3e.SliderThumb.view [ M3e.SliderThumb.value 25 ] []
    , M3e.SliderThumb.view [ M3e.SliderThumb.value 75 ] []
    ]`,
  imports: ["import M3e.Slider", "import M3e.SliderThumb", "import M3e.Values"],
  id: "m3e-slider-range-elm",
  metadata: {
    nestable: true,
  },
}
