// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=59178-4963
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-search-view -> M3e.SearchView.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Search full-screen layout" (59178:4963).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * text content -> Kit.text (userland SEAM, not a library export or a bundle fact; see profiles/m3-kit/profile.json's elm.textSeam — NOT the golden spike's stale "M3e.text").
 * children -> rendered from examples.json / inline example into the M3e.SearchView.component child list (defect D fix).
 * example child (not emitted): <input> slot="input" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * example child (not emitted): <img> slot="leading" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * example child (not emitted): <img> slot="leading" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * example child (not emitted): <img> slot="leading" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * example child (not emitted): <img> slot="leading" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * example child (not emitted): <img> slot="leading" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.SearchView.component
    { content = M3e.List.component [] [ M3e.ListItem.component [] [ Kit.text "Image result one", M3e.ListItem.trailing (M3e.Icon.icon M3e.Icon.northWest [] []) ], M3e.ListItem.component [] [ Kit.text "Image result two", M3e.ListItem.trailing (M3e.Icon.icon M3e.Icon.northWest [] []) ], M3e.ListItem.component [] [ Kit.text "Image result three", M3e.ListItem.trailing (M3e.Icon.icon M3e.Icon.northWest [] []) ], M3e.ListItem.component [] [ Kit.text "Image result four", M3e.ListItem.trailing (M3e.Icon.icon M3e.Icon.northWest [] []) ], M3e.ListItem.component [] [ Kit.text "Image result five", M3e.ListItem.trailing (M3e.Icon.icon M3e.Icon.northWest [] []) ] ]
    , action = M3e.Action.none
    }
    [ M3e.SearchView.contained True
    , M3e.SearchView.mode M3e.Values.fullscreen
    ]
    []`,
  imports: ["import Kit", "import M3e.Action", "import M3e.Icon", "import M3e.List", "import M3e.ListItem", "import M3e.SearchView", "import M3e.Values"],
  id: "m3e-search-view-fullscreen-elm",
  metadata: {
    nestable: true,
  },
}
