// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=52350-27876
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-card -> M3e.Card.view (elmSurface: "top", surface Standard).
 * bound to Figma set "Horizontal card" (52350:27876).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Card.view child list (defect D fix).
 * example child (not emitted): <img> slot="header" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * axis: Style -> variant
 * axis (unmapped): Layout — no CEM enum attribute shares its value set (options: Slot, Media & text)
 * prop: Header text -> content
 * prop (mapped, not bound in this Elm surface): Subhead text -> content (kind text)
 * prop (mapped, not bound in this Elm surface): Title text -> content (kind text)
 * prop (mapped, not bound in this Elm surface): Subtitle text -> content (kind text)
 * prop (mapped, not bound in this Elm surface): Supporting text -> content (kind text)
 * prop (unmapped): Slot swap — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show secondary action — no CEM counterpart (Figma-only property)
 * prop (unmapped): Content — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const variant = instance.getEnum("Style", {
  Outlined: "M3e.Values.outlined",
  Elevated: "M3e.Values.elevated",
  Filled: "M3e.Values.filled",
})

export default {
  example: figma.code`M3e.Card.view
    [ M3e.Card.orientation M3e.Values.horizontal
    , M3e.Card.variant ${variant}
    , M3e.Card.inline ""
    ]
    [ M3e.Card.content (Kit.text "Headline")
    , M3e.Card.content (Kit.text "Supporting text, lorem ipsum dolor sit amet.")
    , M3e.Card.actions (M3e.Button.view [ M3e.Button.variant M3e.Values.text ] [ Kit.text "Action" ])
    , M3e.Card.actions (M3e.Button.view [ M3e.Button.variant M3e.Values.text ] [ Kit.text "Action" ])
    ]`,
  imports: ["import Kit", "import M3e.Button", "import M3e.Card", "import M3e.Values"],
  id: "m3e-card-horizontal-elm",
  metadata: {
    nestable: true,
  },
}
