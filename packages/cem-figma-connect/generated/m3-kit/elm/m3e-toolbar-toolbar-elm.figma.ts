// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=58467-8206
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-toolbar -> M3e.Toolbar.view (elmSurface: "top", surface Standard).
 * bound to Figma set "Toolbar" (58467:8206).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Toolbar.view child list (defect D fix).
 * axis: Color -> variant
 * axis (unmapped): Configuration — no CEM enum attribute shares its value set (options: Floating, Docked)
 * axis (unmapped): Orientation — no CEM enum attribute shares its value set (options: Horizontal, Vertical)
 * prop (unmapped): Content (standard) — no CEM counterpart (Figma-only property)
 * prop (unmapped): Content (vibrant) — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const variant = instance.getEnum("Color", {
  Vibrant: "M3e.Values.vibrant",
  Standard: "M3e.Values.standard",
})

export default {
  example: figma.code`M3e.Toolbar.view
    [ M3e.Toolbar.variant ${variant}
    , M3e.Toolbar.elevated True
    , M3e.Toolbar.shape M3e.Values.rounded
    ]
    [ M3e.IconButton.view [] [ M3e.Icon.view [ M3e.Icon.filled True, M3e.Icon.name "share" ] [] ]
    , M3e.IconButton.view [] [ M3e.Icon.view [ M3e.Icon.filled True, M3e.Icon.name "chat" ] [] ]
    , M3e.IconButton.view [] [ M3e.Icon.view [ M3e.Icon.filled True, M3e.Icon.name "download" ] [] ]
    ]`,
  imports: ["import M3e.Icon", "import M3e.IconButton", "import M3e.Toolbar", "import M3e.Values"],
  id: "m3e-toolbar-toolbar-elm",
  metadata: {
    nestable: true,
  },
}
