// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=58663-30358
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-icon-button -> M3e.IconButton.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Icon button - standard" (58663:30358).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * text content -> Kit.text (userland SEAM, not a library export or a bundle fact; see profiles/m3-kit/profile.json's elm.textSeam — NOT the golden spike's stale "M3e.text").
 * children -> rendered from examples.json / inline example into the M3e.IconButton.component child list (defect D fix).
 * axis: Size -> size
 * axis: Type -> shape
 * axis: Width -> width
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Focused, Pressed, Disabled)
 * prop (mapped, not bound in this Elm surface): Icon -> slot: (kind instanceSwap)
 * prop (unmapped): Show focus indicator — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const size = instance.getEnum("Size", {
  XSmall: "M3e.Values.extraSmall",
  Small: "M3e.Values.small",
  Medium: "M3e.Values.medium",
  Large: "M3e.Values.large",
  XLarge: "M3e.Values.extraLarge",
})

const shape = instance.getEnum("Type", {
  Round: "M3e.Values.rounded",
  Square: "M3e.Values.square",
})

const width = instance.getEnum("Width", {
  Narrow: "M3e.Values.narrow",
  Default: "M3e.Values.default",
  Wide: "M3e.Values.wide",
})

export default {
  example: figma.code`M3e.IconButton.component
    { content = M3e.Icon.icon M3e.Icon.stars [ M3e.Icon.filled True ] []
    , action = M3e.Action.none
    }
    [ M3e.IconButton.variant M3e.Values.standard
    , M3e.IconButton.size ${size}
    , M3e.IconButton.shape ${shape}
    , M3e.IconButton.width ${width}
    ]
    []`,
  imports: ["import M3e.Action", "import M3e.Icon", "import M3e.IconButton", "import M3e.Values"],
  id: "m3e-icon-button-standard-elm",
  metadata: {
    nestable: true,
  },
}
