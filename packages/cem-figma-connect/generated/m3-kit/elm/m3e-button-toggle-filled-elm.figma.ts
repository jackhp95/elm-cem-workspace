// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=57994-2328
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-button -> M3e.Button.view (elmSurface: "top", surface Standard).
 * bound to Figma set "Toggle button" (57994:2328).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Button.view child list (defect D fix).
 * axis: Size -> size
 * axis: Type -> shape
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Focused, Pressed, Disabled)
 * attr (slot, not emitted): selected -> selected is an Elm SLOT on M3e.Button, not a settable bool; omitted (task B).
 * prop: Label text -> content
 * prop (mapped, not bound in this Elm surface): Show icon -> slot:icon (kind boolean)
 * prop (mapped, not bound in this Elm surface): Icon -> slot:icon (kind instanceSwap)
 * prop (unmapped): Show focus indicator — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const size = instance.getEnum("Size", {
  XSmall: "M3e.Values.extraSmall",
  Small: "M3e.Values.small",
  Medium: "M3e.Values.medium",
  Large: "M3e.Values.large",
  XLarge: "M3e.Values.extraLarge",
})

const shape = instance.getEnum("Type", {
  Round: "M3e.Values.rounded",
  Square: "M3e.Values.square",
})

export default {
  example: figma.code`M3e.Button.view
    [ M3e.Button.toggle True
    , M3e.Button.variant M3e.Values.filled
    , M3e.Button.size ${size}
    , M3e.Button.shape ${shape}
    ]
    [ M3e.Button.icon (M3e.Icon.view [ M3e.Icon.name "stars" ] [])
    , Kit.text "Label"
    ]`,
  imports: ["import Kit", "import M3e.Button", "import M3e.Icon", "import M3e.Values"],
  id: "m3e-button-toggle-filled-elm",
  metadata: {
    nestable: true,
  },
}
