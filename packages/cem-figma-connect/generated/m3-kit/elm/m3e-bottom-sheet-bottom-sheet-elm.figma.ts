// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=51827-5859
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-bottom-sheet -> M3e.BottomSheet.view (elmSurface: "top", surface Standard).
 * bound to Figma set "Bottom sheet" (51827:5859).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.BottomSheet.view child list (defect D fix).
 * axis: Modal -> modal
 * prop (unmapped): Show drag handle — no CEM counterpart (Figma-only property)
 * prop (unmapped): Content — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const modal = instance.getEnum("Modal", {
  False: "False",
  True: "True",
})

export default {
  example: figma.code`M3e.BottomSheet.view
    [ M3e.BottomSheet.modal ${modal}
    ]
    []`,
  imports: ["import M3e.BottomSheet"],
  id: "m3e-bottom-sheet-bottom-sheet-elm",
  metadata: {
    nestable: true,
  },
}
