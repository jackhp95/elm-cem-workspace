{
  "$comment": "m3-kit's matcher calibration (finding 2.4, 2026-08-17 thermonuclear review — this data used to be hardcoded as JS constants in the GENERIC src/match/matcher.mjs, contradicting the spine-design doc's promise that kit-specificity stays under profiles/<kit>/). Loaded via matcher.mjs's loadMatcherConfig() and threaded through match()/proposeAxis() as the required matcherConfig parameter — the matcher module itself carries none of this.",
  "descriptionUntrusted": ["m3e-menu-item", "m3e-stepper-previous"],
  "_descriptionUntrusted.$comment": "A4 forward-flag: these two tags' CEM description/module fields describe the WRONG element (an upstream dedup artifact, scoped out of A4). Match them on name only — never let their poisoned description feed the fuzzy scorer.",
  "fuzzyAcceptThreshold": 0.5,
  "_fuzzyAcceptThreshold.$comment": "Calibrated against the checked-in m3-kit fixture (task-A5-report.md): the best-scoring correct fuzzy match (\"Assistive chip\" → m3e-assist-chip, 0.524) sits alone above a noise floor of *wrong* best-matches at ≤0.444 (e.g. \"Radio buttons\" → m3e-split-button). 0.50 sits in that cliff — it admits the one unambiguous semantic win and rejects the noise.",
  "booleanOptionPolarity": [
    ["true", true], ["false", false],
    ["on", true], ["off", false],
    ["yes", true], ["no", false]
  ],
  "_booleanOptionPolarity.$comment": "Which folded Figma option words read as the TRUE vs FALSE pole. Kept minimal + auditable (the three pairs the M3 kit actually uses on its boolean variant axes) — extend deliberately.",
  "booleanAxisSynonyms": [
    ["selected", "checked"]
  ],
  "_booleanAxisSynonyms.$comment": "Axis-name (folded) → the CEM boolean attribute name it denotes, for cases where the Figma axis and the @m3e/web attribute are domain synonyms rather than name-identical. Tiny by design (Figma 'Selected' == the `checked` boolean); grow only as real kit cases demand.",
  "multiBooleanAffinity": [
    ["selected", "checked"],
    ["indeterminate", "indeterminate"]
  ],
  "_multiBooleanAffinity.$comment": "[wordThatImpliesTrue (folded), cemAttrName] pairs for axes that drive MULTIPLE boolean attrs (e.g. checkbox's Type axis). Matched by folded whole-word match (exact or suffix, excluding 'un…' negations) — see affinityMatch() in matcher.mjs. Kept minimal: extend only for real new kit cases."
}
