{
  "$comment": "Synthetic profile for test/emitter-api.test.mjs (finding 2.4 fixture, not m3-kit's calibration — proves loadProfile()/loadMatcherConfig() are genuinely per-profile, not defaulting to M3E's numbers). This profile's own tests don't run fresh matching (they read its correspondence.json directly), so these values are never exercised by an assertion — they only need to exist so loadProfile() succeeds.",
  "descriptionUntrusted": [],
  "fuzzyAcceptThreshold": 0.5,
  "booleanOptionPolarity": [
    ["true", true], ["false", false]
  ],
  "booleanAxisSynonyms": [],
  "multiBooleanAffinity": []
}
