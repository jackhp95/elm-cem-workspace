{
  "$comment": "Synthetic profile for test/emitter-api.test.mjs ONLY (task B2). Points its cem/figma-export inputs at the real, already-validated m3-kit fixtures (test/fixtures/) purely so loadCem/loadFigmaExport succeed — the entries in this profile's own correspondence.json are what's actually under test, not m3-kit's. `emitters` lists only the toy emitter (test/fixtures/toy-profile/emitters/toy.mjs) — html-label's own behavior is covered by test/html-label.test.mjs and is not re-tested here. `deltas` suppresses one cemTag to exercise 'suppressed entries emit nothing' end-to-end.",
  "name": "toy-profile",
  "fileKey": "TOYFILEKEY0000000000000",
  "kitVersionTag": "toy-fixture",
  "figmaExportPath": "test/fixtures/figma-export.m3-kit.json",
  "cem": {
    "package": "@toy/web",
    "version": "0.0.0",
    "manifestPath": "test/fixtures/cem-facts.m3e-web-2.5.14.json"
  },
  "htmlLabel": {},
  "emitters": ["test/fixtures/toy-profile/emitters/toy.mjs"],
  "deltas": [
    { "cemTag": "m3e-assist-chip", "action": "suppress" }
  ]
}
