---
name: m3e-disclosure-hook-design
description: "m3e-okf distributable knowledge package + disclosure hooks — design REV 2 DONE 2026-07-21 (reframed as shareable package, 3 layers, code+chat triggers); impl pending"
metadata: 
  node_type: memory
  type: project
  originSessionId: a7eea8e2-9aec-48f5-993b-08927ebc9068
---

Design doc (rev 2): `planning/execution/2026-07-21-m3e-okf-hook-design.md`. Companion (written first, per reframe): `…-m3e-styling-heuristic.md` (heuristic + frictions doc — destined `m3e-okf/docs/styling-heuristic.md`; hooks summarize/link it, NEVER quote a rule line). Reframe: `…-m3e-okf-REFRAME.md`. Status 2026-07-21: **design complete rev 2, NOT implemented.**

Framing (rev 2 — Jack's correction of rev 1's personal-setup scope): m3e-okf = **distributable, self-contained wiki+skill+hooks package** (= m3e-docs renamed) for ANY adopter; 3 knowledge layers: Material spec (`knowledge/` OKF bundle) / @m3e/web (cards + hook index) / elm-m3e (`implementations/elm-m3e/`, new). General (no Jack-specifics), context-frugal, disclose-once.

Kept mechanics (rev 1, verified good):
- **Crux solved:** subagent tool calls carry `agent_id` (absent on main loop) → state key `<session_id>/<agent_id ?? "main">.json`; stateRoot COMPUTED ($CLAUDE_PLUGIN_DATA > XDG > ~/.claude/hook-state/m3e-okf). PreCompact deletes key; SessionEnd + mtime GC.
- **Data source NOT raw CEM** (145 named enum types unresolved there) — `data/components.json` (extract.mjs, unions inlined) → `build-hook-index.mjs` → `skills/m3e/hook-index.json`, both flavors (web + derived Elm names cross-checked vs M3e.* exposing lists fail-loud), rides CI guard. Build-time-only elm-m3e dep.
- Levels: L1 catalog ~400tok (ANY UI-file edit incl .css in m3e repo — unaware-agent case; carries one-line escalation-ladder summary + heuristic-doc pointer) / L2 attrs+slots, ≤6-value enums inlined / L3 big vocabs only. Max 3 components/invocation.

New in rev 2:
- **Chat trigger** (UserPromptSubmit, L0 ~100tok, precision-biased: strong tokens only — m3e/@m3e/web/elm-m3e/material design/material 3/m3 expressive/material you; component names NEVER fire; once per window; L1 suppresses L0; code hook = recall backstop).
- **Distribution:** channel A = settings snippets (project + user, tradeoffs documented — ADOPTER's choice, not Jack's); channel B = Claude Code plugin (recommended target; $CLAUDE_PLUGIN_ROOT/$CLAUDE_PLUGIN_DATA; Phase 0 verifies plugin-hook parity).
- Jack answered: SubagentStart L1 = NO (recorded as rejected); verbatim rule quote = NO.

Phases: P0 empirical hook-fact spike (sonnet/high) → P1 index → P2 code hook L1 → P3 L2/L3+chat → P4 lifecycle+packaging+INSTALL.md (sonnet/medium each) → P5 soak/tune+review (opus/medium). Extension parked: PostToolUse deterministic attr-lint. OQs: elm-m3e dep name for detection; elm-m3e layer depth; npm identity; chat-token list as code vs adopter knob.

**First dogfood DONE (Jack 2026-07-21):** cross-app SEAM REVIEW of the 3 wave apps (feedback-fab `efe7878` / compass-social `3aae2ab` / animal-spirits `c8ea175`, on `m3e-migration` branches, unpushed) — proxy dogfood (styling-heuristic + `data/components.json` as lens; mechanism still impl-pending). Read-only findings report delivered. **m3e-okf BUILD SPEC (the 5 catalog/heuristic frictions = the real payoff):** (1) **styling-heuristic missing a PRODUCTION axis** — its ladder is all about *where visual values come from* (styling), silent on *how markup is produced*; that blind spot HID the headline finding (R1). Needs a rung-0 / parallel note: "build plain elements via the typed producer layer (`TypedHtml.*` in elm-m3e) — CLOSED element-natural rows, no escape hatch; reserve `HtmlIr.Internal`/`Native.*` forgers (the seam) ONLY for what typed can't express: custom elements, event escapes, arbitrary attr/style." (2) `components.json` has **no composition/host contract** (nothing says `m3e-form-field` wants native `<label for>`+`<input id>` siblings + does no id-wiring — all 3 apps reverse-engineered it). (3) **no block-vs-inline** flag (blocks the typography→`m3e-heading` refactor). (4) **no navigable/link-attr flag** (m3e-button/icon-button/nav-item/list-item carry native `href` — don't wrap them in a native anchor). (5) **no disposition for cross-app seam duplication** (promote-to-shared-kit cue). **Refactor findings (app-side, NOT yet executed, Jack to prioritize):** R1 (headline) `Native.*` re-implements `TypedHtml.*` — same redundancy class as removed idWiring/ariaLabel; retarget Native→TypedHtml, Native collapses to escapes+custom+arbitrary (FF+compass). R2 form-field pattern rederived 3× → one shared kit helper. R3 text/link atoms dup'd → `TypedHtml.text` + one kit link. M1 compass `Kit` typography→`m3e-heading`/typescale (blocked on friction #3). M2 link-wrapping-component redundant w/ native `href`; `Layout.button`→`M3e.Button`. **animal-spirits' minimal `Seam` (html+text only, no Native) is EXEMPLARY — the target shape.** Cem rules cover the enforceable slice (compass: 3× `M3e.icon` in button DEFAULT slot → `M3e.Button.icon`, zero-suppress); the seam review adds the redundancy/refactor lens rules can't express. **SOLIDIFICATION DONE 2026-07-21 (all merged to respective mains, local/unpushed) — the build spec is now IMPLEMENTED:** (A) **`NoRedundantElementForge`** elm-review-cem rule (main `d310e36`; import-gated declaration-body traversal, flags a producer forging a literal tag ∈ `TypedHtml.Review.Facts` covered-set, skips node/custom/dynamic/escapes, advisory no-autofix; **T-A4 caught 24 real redundant producers** in feedback-fab's Native+Seam). (B) catalog `navigable`(13)/`display`(110, TS `:host` derive + 6-elt overlay)/`hostContract`(form-field seed) in m3e-docs `extract.mjs` + surfaced in cards (main `b430d48`). (C) production-axis doc (elm-m3e layer, `planning/execution/2026-07-21-m3e-production-axis.md`) + styling-heuristic pointer (orthogonal, NOT a ladder rung) + friction #9 (cross-app-dedup). Reference kit fixed: `Native`→escapes-only, `Seam` atoms→TypedHtml-based, shared `Seam.field` helper (elm-m3e main `4b5f5ec`; kids coerced via `List.map recast`). Plan: `planning/execution/2026-07-21-m3e-systems-solidification-plan.md`. Two sonnet agents narration-yielded (B no-op → opus retry; A2 stopped pre-verify → I picked up the Seam closed-child-row compile fix + MIGRATION.md). App retargets (apply the rule's guidance to the 3 apps) deliberately deferred. **NEXT: release plan → archive+squash (Jack-gated).**

Related: [[m3e-components-for-styling]] (motivating rule — now generalized into the heuristic doc), [[graphify-per-repo-graphs]], [[elmq-elm-refactor-cli]] (elmq drives the mechanical seam refactors).
