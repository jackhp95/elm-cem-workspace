{
  "_phantom": true,
  "_brand": "Cf",
  "_comment": [
    "The `datetime` regression, minimised. Two elements declare one attribute at two",
    "SCALAR types (string vs number) and share ONE `home` module.",
    "",
    "A home module emits a single re-exported setter per attribute name, so there is no",
    "output that could be correct: the generator must refuse rather than dedupe the two",
    "specs down to one and publish the survivor's type for both. In elm-typed-html it did",
    "the latter — <time>'s Float won, and `<ins datetime=\"2024-01-01\">` became",
    "unexpressible with no diagnostic anywhere.",
    "",
    "Remove the `home` keys and this fixture STOPS failing: cross-module conflicts are",
    "legitimate and handled (each element keeps a locally-typed setter). The home is the",
    "load-bearing part of the reproduction."
  ],
  "Ins": {
    "home": "Prose",
    "admits": { "unnamed": { "kinds": [], "multi": false } }
  },
  "Time": {
    "home": "Prose",
    "admits": { "unnamed": { "kinds": [], "multi": false } }
  }
}
