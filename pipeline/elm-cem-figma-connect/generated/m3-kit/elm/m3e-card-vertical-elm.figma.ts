// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=52346-27573
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-card -> M3e.Element.Card.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Stacked card" (52346:27573).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Element.Card.component child list (defect D fix).
 * example child (not emitted): <img> — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * axis: Style -> variant
 * axis (unmapped): Layout — no CEM enum attribute shares its value set (options: Slot, Media & text)
 * prop: Header text -> content
 * prop (mapped, not bound in this Elm surface): Subhead text -> content (kind text)
 * prop (mapped, not bound in this Elm surface): Title text -> content (kind text)
 * prop (mapped, not bound in this Elm surface): Subtitle text -> content (kind text)
 * prop (mapped, not bound in this Elm surface): Supporting text -> content (kind text)
 * prop (unmapped): Slot swap — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show secondary action — no CEM counterpart (Figma-only property)
 * prop (unmapped): Content — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

const variant = instance.getEnum("Style", {
  Outlined: "M3e.Values.outlined",
  Elevated: "M3e.Values.elevated",
  Filled: "M3e.Values.filled",
})

export default {
  example: figma.code`M3e.Element.Card.component
    [ M3e.Element.Card.orientation M3e.Values.vertical
    , M3e.Element.Card.variant ${variant}
    ]
    [ M3e.Element.Card.header (M3e.Element.Avatar.component [] [])
    , M3e.Element.Card.header (M3e.Element.IconButton.component { content = M3e.Icon.icon M3e.Icon.moreVert [] [], action = M3e.Action.none } [] [])
    , M3e.Element.Card.content (M3e.text "Title")
    , M3e.Element.Card.content (M3e.text "Subtitle")
    , M3e.Element.Card.content (M3e.text "Supporting text, lorem ipsum dolor sit amet.")
    , M3e.Element.Card.actions (M3e.Element.Button.component { content = M3e.text "Secondary", action = M3e.Action.none } [ M3e.Element.Button.variant M3e.Values.text ] [])
    , M3e.Element.Card.actions (M3e.Element.Button.component { content = M3e.text "Primary", action = M3e.Action.none } [ M3e.Element.Button.variant M3e.Values.text ] [])
    ]`,
  imports: ["import M3e", "import M3e.Action", "import M3e.Element.Avatar", "import M3e.Element.Button", "import M3e.Element.Card", "import M3e.Element.IconButton", "import M3e.Icon", "import M3e.Values"],
  id: "m3e-card-vertical-elm",
  metadata: {
    nestable: true,
  },
}
