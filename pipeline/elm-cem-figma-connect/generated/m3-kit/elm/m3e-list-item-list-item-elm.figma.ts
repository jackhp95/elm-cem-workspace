// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=59106-13183
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-list-item -> M3e.Element.ListItem.component (elmSurface: "top", surface Standard).
 * bound to Figma set "List item" (59106:13183).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Element.ListItem.component child list (defect D fix).
 * axis (unmapped): Alignment — no CEM enum attribute shares its value set (options: Middle-aligned, Top-aligned)
 * axis (unmapped): State — no CEM enum attribute shares its value set (options: Enabled, Hovered, Focused, Pressed, Dragged, Disabled)
 * axis (unmapped): Selected — no CEM enum attribute shares its value set (options: False, True)
 * prop (unmapped): Show trailing element — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show leading element — no CEM counterpart (Figma-only property)
 * prop (unmapped): Show focus indicator — no CEM counterpart (Figma-only property)
 * prop (unmapped): Content — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.Element.ListItem.component
    [ 
    ]
    [ M3e.Element.ListItem.leading (M3e.Icon.icon M3e.Icon.stars [ M3e.Element.Icon.filled True ] [])
    , M3e.Element.ListItem.overline (M3e.text "Overline")
    , M3e.text "Label text"
    , M3e.Element.ListItem.supportingText (M3e.text "Supporting line text, lorem ipsum dolor")
    , M3e.Element.ListItem.trailing (M3e.text "⌘C")
    , M3e.Element.ListItem.trailing (M3e.Icon.icon M3e.Icon.chevronRight [] [])
    ]`,
  imports: ["import M3e", "import M3e.Element.Icon", "import M3e.Element.ListItem", "import M3e.Icon"],
  id: "m3e-list-item-list-item-elm",
  metadata: {
    nestable: true,
  },
}
