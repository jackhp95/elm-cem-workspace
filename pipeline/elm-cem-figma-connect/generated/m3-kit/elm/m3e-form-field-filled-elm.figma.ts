// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=52798-24373
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-form-field -> M3e.Element.FormField.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Text field" (52798:24373).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Element.FormField.component child list (defect D fix).
 * example child (not emitted): <label> slot="label" — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 * example child (not emitted): <input> — opaque HTML element with no verified elm-m3e element seam; the Web Components emitter renders it, the Elm emitter has no verified seam.
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.Element.FormField.component
    [ M3e.Element.FormField.variant M3e.Values.filled
    ]
    [ M3e.Element.FormField.suffix (M3e.Icon.icon M3e.Icon.cancel [] [])
    , M3e.Element.FormField.hint (M3e.text "Supporting text")
    ]`,
  imports: ["import M3e", "import M3e.Element.FormField", "import M3e.Icon", "import M3e.Values"],
  id: "m3e-form-field-filled-elm",
  metadata: {
    nestable: true,
  },
}
