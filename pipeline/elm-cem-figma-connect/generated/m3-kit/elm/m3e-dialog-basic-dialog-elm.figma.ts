// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=50723-10929
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-dialog -> M3e.Element.Dialog.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Basic dialog" (50723:10929).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * children -> rendered from examples.json / inline example into the M3e.Element.Dialog.component child list (defect D fix).
 * axis (unmapped): Icon — no CEM enum attribute shares its value set (options: True, False)
 * prop: Title -> content
 * prop (mapped, not bound in this Elm surface): Supporting text -> content (kind text)
 * prop (unmapped): Show divider — no CEM counterpart (Figma-only property)
 * prop (unmapped): Content — no CEM counterpart (Figma-only property)
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.Element.Dialog.component
    [ 
    ]
    [ M3e.Element.Dialog.header (M3e.text "Basic dialog title")
    , M3e.text "A dialog is a type of modal window that appears in front of app content to provide critical information, or prompt for a decision to be made."
    , M3e.Element.Dialog.actions (M3e.Element.Button.component { content = M3e.text "Cancel", action = M3e.Action.none } [ M3e.Element.Button.variant M3e.Values.text ] [])
    , M3e.Element.Dialog.actions (M3e.Element.Button.component { content = M3e.text "OK", action = M3e.Action.none } [ M3e.Element.Button.variant M3e.Values.text ] [])
    ]`,
  imports: ["import M3e", "import M3e.Action", "import M3e.Element.Button", "import M3e.Element.Dialog", "import M3e.Values"],
  id: "m3e-dialog-basic-dialog-elm",
  metadata: {
    nestable: true,
  },
}
