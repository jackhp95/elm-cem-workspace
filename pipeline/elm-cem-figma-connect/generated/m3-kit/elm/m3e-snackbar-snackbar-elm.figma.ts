// url=https://www.figma.com/design/UtwpUdPiOZEuxp8Nq1d5yQ/Material-3-Design-Kit--Community-?node-id=53977-33575
import figma from "figma"

/**
 * GENERATED by cem-figma-connect (profiles/m3-kit/emitters/elm.mjs) — do not edit by hand.
 * m3e-snackbar -> M3e.Element.Snackbar.component (elmSurface: "top", surface Standard).
 * bound to Figma set "Snackbar" (53977:33575).
 * token names resolved from the elm-cem facts bundle Face C (profiles/m3-kit/facts/elm-api-facts.json); provenance stamp lives in that file's provenance object; NOT the golden's stale short names.
 * text content -> M3e.text (userland SEAM, not a library export or a bundle fact; see profiles/m3-kit/profile.json's elm.textSeam — NOT the golden spike's stale "M3e.text").
 * children -> rendered from examples.json / inline example into the M3e.Element.Snackbar.component child list (defect D fix).
 * axis (unmapped): Configuration — no CEM enum attribute shares its value set (options: Text only, Text & action, Text & longer action)
 * axis (unmapped): # of lines — no CEM enum attribute shares its value set (options: One line, Two lines)
 * axis (unmapped): Show close affordance — no CEM enum attribute shares its value set (options: False, True)
 * prop: Supporting text -> content
 */

const instance = figma.selectedInstance

export default {
  example: figma.code`M3e.Element.Snackbar.component
    { content = M3e.text "Snackbar supporting text"
    , action = M3e.Action.none
    }
    [ 
    ]
    []`,
  imports: ["import M3e", "import M3e.Action", "import M3e.Element.Snackbar"],
  id: "m3e-snackbar-snackbar-elm",
  metadata: {
    nestable: true,
  },
}
